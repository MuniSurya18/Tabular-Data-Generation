
# Install PyTorch with CPU support to avoid path length issues on Windows:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
numpy
pandas
scikit-learn
tqdm
pypdf  # If we need to process pdfs again
matplotlib
seaborn
streamlit
plotly
altair
pyngrok
